# Simple Calculator (Flutter)

A small calculator app implemented in Flutter.

Features
- Basic arithmetic: + - × ÷
- Decimal support
- Percent, plus/minus, clear
- Simple dark theme

Run
1. Open a terminal and change to the project folder:

```powershell
Set-Location -LiteralPath 'C:\Users\SOHAM\OneDrive\Desktop\appdev\flutter-calculatorapp\flutter_application_1'
```

2. Get dependencies and run:

```powershell
flutter pub get
flutter run
```

Notes
- Requires Flutter SDK (stable). I detected Flutter on your PATH already.
- If you want packaging or platform-specific builds, tell me and I can add instructions.
