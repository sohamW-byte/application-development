 # CRUD on Array (JavaScript Example)

This project demonstrates basic **CRUD operations** (Create, Read, Update, Delete) on an array using JavaScript.

## Steps

1. Run the program with:
   ```bash
   node index.js
